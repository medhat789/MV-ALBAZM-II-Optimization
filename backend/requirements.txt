# M/V Al-bazm II — production runtime requirements
# Pinned versions are intentional (matches what was tested in dev)

# Web framework
fastapi==0.110.1
uvicorn==0.25.0
pydantic==2.13.4
python-dotenv==1.0.1
python-multipart==0.0.9

# HTTP client (for Open-Meteo)
httpx==0.28.1

# Data + ML
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
scipy==1.17.1
joblib==1.5.3

# Excel readers (used by the data-prep utility scripts)
openpyxl==3.1.5

# Optional Mongo driver — kept because route_manager imports it indirectly
# in some envs; harmless if unused
motor==3.3.1
pymongo==4.6.0
